# Sundial — notifications service

Delivers workspace notifications (mentions, comment replies, agent-run completions) across
push, email, and in-app channels. The product spec lives in the Sundial workspace
(`notifications-spec.md`); this repo implements it.
